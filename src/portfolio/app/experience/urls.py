# from django.urls import path

# from .views import ExperienceViewSet


app_name = 'experience'

urlpatterns = [
    # path('', ExperienceViewSet.as_view({
    #     'get': 'list',
    #     'post': 'create',
    # })),
]
